{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Debug with CodeLLDB",
      "type": "lldb",
      "request": "launch",
      "program": "${workspaceFolder}/bin/space_invaders",
      "cwd": "${workspaceFolder}",
      "preLaunchTask": "make all",
      "stopOnEntry": false
    }
  ]
}